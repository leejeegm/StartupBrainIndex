# SBI 배포/로컬 환경 변수 예시
# 사용법: 이 파일을 복사해 .env 또는 배포 플랫폼 Environment Variables에 넣고, 값을 채운다.
# .env 는 .gitignore에 있으므로 저장소에 올라가지 않는다.

# 서버 포트 (Render 등은 PORT 자동 지정)
PORT=8000

# 세션 암호화 (배포 시 반드시 설정 권장. 미설정 시 재시작마다 세션 초기화)
# 생성: python -c "import secrets; print(secrets.token_hex(32))"
SESSION_SECRET=your-random-secret-here

# OpenAI API 키 (선택)
# - Python: Step 4 AI 상담. 배포(Render 등)에서는 Environment Variables에 OPENAI_API_KEY 설정.
# - PHP: generate_description.php, generate_image.php, regenerate_and_update.php. 로컬은 .env에 아래 한 줄 추가.
OPENAI_API_KEY=

# DB: sqlite(로컬/휘발성) | postgres(권장·재배포 시 데이터 유지) | mysql
DB_ENGINE=sqlite
DB_NAME=sbi.db

# ----- 재배포 시에도 회원·프로필·저장 데이터 유지 (권장) -----
# Render: Dashboard → Web Service → Environment → Add Environment Variable
# 1) Render PostgreSQL: Dashboard → New → PostgreSQL → 생성 후 Web Service에 연결하면 DATABASE_URL 자동 주입
# 2) 아래만 추가:
# DB_ENGINE=postgres
# (DATABASE_URL은 Render가 PostgreSQL 추가 시 자동 설정)
#
# Supabase 등 외부 Postgres 사용 시:
# DB_ENGINE=postgres
# DATABASE_URL=postgresql://user:password@host:5432/dbname
# (또는 SUPABASE_DB_URL=...)

# MySQL 사용 시 (닷홈·외부 DB)
# DB_ENGINE=mysql
# DB_HOST=localhost
# DB_USER=your_db_user
# DB_PASSWORD=your_db_password
# DB_NAME=your_db_name
