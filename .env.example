# SBI 배포/로컬 환경 변수 예시
# 사용법: 이 파일을 복사해 .env 또는 배포 플랫폼 Environment Variables에 넣고, 값을 채운다.
# .env 는 .gitignore에 있으므로 저장소에 올라가지 않는다.

# 서버 포트 (Render 등은 PORT 자동 지정)
PORT=8000

# 세션 암호화 (배포 시 반드시 랜덤 문자열로 설정)
SESSION_SECRET=your-random-secret-here

# OpenAI API (PHP 이미지/설명 생성 스크립트용, 선택)
# OPENAI_API_KEY=sk-proj-...

# DB: sqlite(무료 단일 파일) 또는 mysql
DB_ENGINE=sqlite
DB_NAME=sbi.db

# MySQL 사용 시에만 아래 채우기 (닷홈·외부 DB)
# DB_ENGINE=mysql
# DB_HOST=localhost
# DB_USER=your_db_user
# DB_PASSWORD=your_db_password
# DB_NAME=your_db_name
